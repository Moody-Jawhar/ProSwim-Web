# Production/test build settings. The portal is served from the V27_WEB IIS
# application and calls the API at the sibling V27_API application.
# Note VITE_* values are inlined into the client bundle — nothing secret here.
VITE_API_BASE_URL=/V27_API
VITE_API_KEY=dev-api-key-12345
