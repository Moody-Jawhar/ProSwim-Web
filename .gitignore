# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ProSwim portal
.env
.env.*
!.env.example
# Deploy-path settings for the V27_WEB application. VITE_* values are inlined
# into the client bundle, so these are public by nature — tracked so a server
# `git pull && npm run build` produces a correctly-pathed build.
!.env.production
/test-results
/playwright-report
shot.tmp.mjs
